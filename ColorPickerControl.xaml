<UserControl x:Class="GaokaoCountdown.ColorPickerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Width="300" Height="380">
    <Border CornerRadius="10" Background="#FF1E1E3A" BorderBrush="#40FFFFFF" BorderThickness="1"
            Padding="0">
        <Border.Effect>
            <DropShadowEffect Color="#000000" BlurRadius="25" ShadowDepth="4" Opacity="0.6"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="34"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="44"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#12FFFFFF" CornerRadius="10,10,0,0">
                <TextBlock Text="选择颜色" FontSize="13" FontWeight="SemiBold"
                           Foreground="#FFE0E0F0" VerticalAlignment="Center" Margin="14,0,0,0"/>
            </Border>

            <StackPanel Grid.Row="1" Margin="14,8,14,4" Orientation="Horizontal">
                <Border Width="36" Height="36" CornerRadius="4" BorderBrush="#30FFFFFF" BorderThickness="1"
                        Background="#222222">
                    <Rectangle x:Name="PreviewRect" RadiusX="3" RadiusY="3"/>
                </Border>
                <TextBox x:Name="HexBox" Text="#FFFFFF" Width="100" Margin="8,0,0,0" VerticalAlignment="Center"
                         FontSize="13" Background="#22FFFFFF" Foreground="White"
                         BorderBrush="#30FFFFFF" BorderThickness="1" Padding="6,3"/>
            </StackPanel>

            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Margin="12,0,6,0">
                <Grid x:Name="PaletteGrid" Margin="0,2"/>
            </ScrollViewer>

            <TextBlock Grid.Row="3" Text="点击色块选择，或直接输入 HEX 值" FontSize="11"
                       Foreground="#66CCCCDD" Margin="14,2,0,2"/>

            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right"
                        VerticalAlignment="Center" Margin="0,0,14,0">
                <Button x:Name="BtnOk" Content="确定" Width="60" Margin="3,0"
                        Background="#336688CC" Foreground="White"/>
                <Button x:Name="BtnCancel" Content="取消" Width="60" Margin="3,0"
                        Background="#22FFFFFF" Foreground="White"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
