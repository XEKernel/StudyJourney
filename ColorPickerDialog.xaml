<Window x:Class="GaokaoCountdown.ColorPickerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="选择颜色" Height="400" Width="320"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ResizeMode="NoResize" ShowInTaskbar="False"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">
    <Border CornerRadius="10" Background="#FF1A1A32" BorderBrush="#30FFFFFF" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect Color="#000000" BlurRadius="20" ShadowDepth="4" Opacity="0.5"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="36"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="46"/>
            </Grid.RowDefinitions>

            <!-- 标题栏 -->
            <Border Grid.Row="0" Background="#10FFFFFF" CornerRadius="10,10,0,0">
                <TextBlock Text="选择颜色" FontSize="13" FontWeight="SemiBold"
                           Foreground="#FFE0E0F0" VerticalAlignment="Center" Margin="16,0,0,0"/>
            </Border>

            <!-- 预览 + HEX -->
            <StackPanel Grid.Row="1" Margin="16,10,16,6" Orientation="Horizontal">
                <Border Width="40" Height="40" CornerRadius="4" BorderBrush="#30FFFFFF" BorderThickness="1"
                        Background="#222222">
                    <Rectangle x:Name="PreviewRect" RadiusX="3" RadiusY="3"/>
                </Border>
                <TextBox x:Name="HexBox" Text="#FFFFFF" Width="100" Margin="10,0,0,0" VerticalAlignment="Center"
                         FontSize="13" Background="#22FFFFFF" Foreground="White"
                         BorderBrush="#30FFFFFF" BorderThickness="1" Padding="6,3"/>
            </StackPanel>

            <!-- 颜色网格 -->
            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Margin="16,0,8,0">
                <Grid x:Name="PaletteGrid" Margin="0,2">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="36"/><RowDefinition Height="36"/>
                        <RowDefinition Height="36"/><RowDefinition Height="36"/>
                        <RowDefinition Height="36"/><RowDefinition Height="36"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="36"/><ColumnDefinition Width="36"/>
                        <ColumnDefinition Width="36"/><ColumnDefinition Width="36"/>
                        <ColumnDefinition Width="36"/><ColumnDefinition Width="36"/>
                    </Grid.ColumnDefinitions>
                </Grid>
            </ScrollViewer>

            <!-- 提示 -->
            <TextBlock Grid.Row="3" Text="点击色块选择，或直接输入 HEX 值" FontSize="11"
                       Foreground="#66CCCCDD" Margin="16,4,0,4"/>

            <!-- 按钮 -->
            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right"
                        VerticalAlignment="Center" Margin="0,0,16,0">
                <Button x:Name="BtnOk" Content="确定" Width="64" Margin="4,0"
                        Background="#336688CC"/>
                <Button x:Name="BtnCancel" Content="取消" Width="64" Margin="4,0"
                        Background="#22FFFFFF"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
